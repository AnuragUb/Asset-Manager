# ops/maintenance/

Reserved for operational maintenance scripts that are not deployment, replication, or health checks.

Examples (to be moved in a later Phase 2 step, not Phase 2.1):

- Root `check_*.js` / `fix_*.js` / `ghost_*.js` one-offs
- `sync_to_nvme.ps1` (backup sync)
- Contents of top-level `scripts/` after curation

This folder is intentionally empty of scripts in Phase 2.1.
